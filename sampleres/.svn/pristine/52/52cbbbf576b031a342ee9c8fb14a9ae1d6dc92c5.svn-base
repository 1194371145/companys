<?php
//sample
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "localhost";//MYSQL服务器地址
$dbname   = "sample";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";


//insidesales
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "192.168.5.16";//MYSQL服务器地址
$dbname   = "insidesales";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";


//scrm
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "localhost";//MYSQL服务器地址
$dbname   = "scrm";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";

//silergypm
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "localhost";//MYSQL服务器地址
$dbname   = "silergypm";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";


//erpm
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "localhost";//MYSQL服务器地址
$dbname   = "erpm";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";


// silergywgcdb
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "localhost";//MYSQL服务器地址
$dbname   = "silergywgcdb";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";

// silergywgcdb
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "localhost";//MYSQL服务器地址
$dbname   = "performance";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";

//webvipsystem
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "localhost";//MYSQL服务器地址
$dbname   = "webvipsystem";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";


//budget
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "192.168.5.16";//MYSQL服务器地址
$dbname   = "silergybudget";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";


//develop
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "192.168.5.16";//MYSQL服务器地址
$dbname   = "develop";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";

//investment
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "192.168.5.16";//MYSQL服务器地址
$dbname   = "investment";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";

//testserver
$username = "root";//你的MYSQL用户名
$password = "Silergy13";//密码
$hostname = "192.168.5.16";//MYSQL服务器地址
$dbname   = "testserver";//数据库名
$dumpfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".sql";
$command = "D:\\wamp\\bin\mysql\\mysql5.6.17\\bin\\mysqldump --add-drop-table --host=$hostname --user=$username ";
if ($password) $command.= "--password=". $password ." ";
$command.= $dbname;
$command.= " > " . $dumpfname;
system($command);
// 压缩成ZIP文件
$zipfname = "D:\\wamp\\mysqlbackup\\".$dbname . "_" . date("Y-m-d_H-i-s").".zip";
$zip = new ZipArchive();
if($zip->open($zipfname,ZIPARCHIVE::CREATE))
{
   $zip->addFile($dumpfname,$dumpfname);
   $zip->close();
}
echo "backup $dbname success ";
?>




